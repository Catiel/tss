module com.simulation {
    requires javafx.controls;
    requires javafx.fxml;
    requires javafx.swing;
    requires org.kordamp.ikonli.javafx;
    // Las siguientes dependencias se usan desde el classpath porque no son modulares:
    // ControlsFX, Ikonli packs, Commons Math, SSJ, JFreeChart
}

